#include <iostream>
#include <cstdio>
#include <vector>
#include <cstdlib>

using namespace std;

struct node {
  int x, y;
  node *l, *r;
};

typedef node * pnode;

void split(pnode t, int x, pnode &l, pnode &r) {
  if (t) {
    if (t->)
  }
}
